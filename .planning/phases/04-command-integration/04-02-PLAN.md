---
phase: 04-command-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - commands/gsd/new-project.md
autonomous: true

must_haves:
  truths:
    - "Brownfield project runs map-codebase inline (no exit-and-return)"
    - "Brownfield project runs brownfield-flow inline with purpose routing"
    - "Brownfield project writes PROJECT.md with Validated requirements from codebase"
    - "Brownfield project skips Phases 3, 4, 6, 7, 8 and converges at Phase 5"
    - "Greenfield project executes Phases 3-10 unchanged (STATE-03 regression safety)"
    - "Phase 10 Done banner adapts artifacts dynamically for both paths"
  artifacts:
    - path: "commands/gsd/new-project.md"
      provides: "Phase 2B brownfield pipeline + convergence guards + dynamic done banner"
      contains: "Phase 2B: Brownfield Pipeline"
  key_links:
    - from: "Phase 2 MODE==brownfield routing"
      to: "Phase 2B Step 1 map-codebase"
      via: "Control flow continuation"
      pattern: "MODE.*brownfield"
    - from: "Phase 2B Step 2 brownfield-flow"
      to: "Phase 5 Workflow Preferences"
      via: "Phase jump (skip Phases 3-4, 6-8)"
      pattern: "Skip to Phase 5"
    - from: "Phase 2B brownfield-flow"
      to: ".planning/brownfield-analysis.md, ROADMAP.md, STATE.md, REQUIREMENTS.md"
      via: "Workflow artifact production"
      pattern: "brownfield-flow"
    - from: "Phase 10 Done banner"
      to: "Dynamic artifact detection"
      via: "File existence checks"
      pattern: "brownfield-analysis.md"
---

<objective>
Add the brownfield pipeline (Phase 2B), convergence guards on Phases 6-8, and dynamic Phase 10 Done banner to `commands/gsd/new-project.md`.

Purpose: This completes the brownfield integration so that `/gsd:new-project` on a directory with existing code automatically maps the codebase, runs brownfield analysis + purpose routing, writes PROJECT.md, and converges at Phase 5 -- all in a single command session. Combined with Plan 01's detection and routing, this fulfills INFRA-04 (brownfield branching integration) and STATE-03 (greenfield unchanged).

Output: Fully modified `commands/gsd/new-project.md` with complete brownfield path from detection through done banner.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-command-integration/04-RESEARCH.md
@.planning/phases/04-command-integration/04-01-SUMMARY.md
@commands/gsd/new-project.md
@get-shit-done/workflows/brownfield-flow.md
@get-shit-done/workflows/map-codebase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 2B Brownfield Pipeline</name>
  <files>commands/gsd/new-project.md</files>
  <action>
  Insert a new "## Phase 2B: Brownfield Pipeline" section in `commands/gsd/new-project.md` BETWEEN Phase 2 (Mode Routing) and Phase 3 (Deep Questioning). Replace the placeholder comment left by Plan 01.

  Phase 2B contains 4 steps. This phase runs ONLY when MODE == "brownfield".

  **Step 1: Map Codebase**

  Check if codebase is already mapped:
  - If HAS_CODEBASE_MAP == "yes": Display "Using existing codebase map." Skip mapping.
  - If HAS_CODEBASE_MAP == "no":

  Display banner:
  ```
  GSD > MAPPING CODEBASE
  ```

  Execute the map-codebase workflow inline by following:
  `@~/.claude/get-shit-done/workflows/map-codebase.md`

  The workflow spawns 4 parallel mapper agents. Wait for completion. Verify 7 documents exist in .planning/codebase/.

  If map-codebase workflow offers "Refresh/Update/Skip" (because .planning/codebase/ already exists), choose "Skip" to use existing map.

  **Step 2: Run Brownfield Analysis + Purpose Routing**

  Display banner:
  ```
  GSD > ANALYZING CODEBASE
  ```

  Execute the brownfield-flow workflow inline by following:
  `@~/.claude/get-shit-done/workflows/brownfield-flow.md`

  The workflow runs with `purpose_routing: true` (default). It executes Steps 1-9:
  - Steps 1-5: Analysis pipeline (prerequisites, scope, analysis via Task(), dashboard, result)
  - Steps 6-9: Purpose routing (purpose selection, questioning, debug bridge, roadmap generation)

  CRITICAL: brownfield-flow uses AskUserQuestion for interactive steps (Steps 6-7). It MUST execute inline within this command context. Do NOT spawn it as Task().

  After brownfield-flow completes, these artifacts exist:
  - .planning/brownfield-analysis.md (analysis summary)
  - .planning/ROADMAP.md (purpose-aware roadmap)
  - .planning/STATE.md (with purpose recorded)
  - .planning/REQUIREMENTS.md (with item-to-phase mapping)

  **Step 3: Write PROJECT.md (Brownfield)**

  Read .planning/codebase/ARCHITECTURE.md and STACK.md. Infer Validated requirements from existing code capabilities.

  Use the existing brownfield PROJECT.md writing logic already present in Phase 4 of new-project.md (the "For brownfield projects (codebase map exists):" section). This logic reads ARCHITECTURE.md and STACK.md, identifies what the codebase already does, and writes these as Validated requirements.

  Also incorporate the brownfield analysis context:
  - Add "Codebase Mode: brownfield" and "Primary Language: {PRIMARY_LANG}" to the Context section
  - Reference .planning/brownfield-analysis.md in the Context section

  Write .planning/PROJECT.md using the template from `templates/project.md`.

  Commit PROJECT.md:
  ```bash
  mkdir -p .planning
  git add .planning/PROJECT.md
  git commit -m "docs: initialize brownfield project

  [One-liner from analysis health + purpose]"
  ```

  **Step 4: Continue to Phase 5**

  Display transition message:
  ```
  Brownfield pipeline complete. Continuing to workflow preferences...
  ```

  Skip directly to Phase 5 (Workflow Preferences). Do NOT execute:
  - Phase 3 (Deep Questioning) -- brownfield-flow handled questioning
  - Phase 4 (Write PROJECT.md) -- handled in Step 3 above
  - Phase 6 (Research Decision) -- brownfield analysis serves as domain research
  - Phase 7 (Define Requirements) -- brownfield-flow produced REQUIREMENTS.md
  - Phase 8 (Create Roadmap) -- brownfield-flow produced ROADMAP.md

  Implementation note: Add a guard instruction before Phase 3 that says:
  "**If MODE == 'brownfield': Phase 2B has already been executed. Skip to Phase 5 (Workflow Preferences).**"

  This ensures the brownfield path jumps correctly even if the executing agent reads the document sequentially.
  </action>
  <verify>
  Read commands/gsd/new-project.md and verify:
  1. "## Phase 2B: Brownfield Pipeline" section exists between Phase 2 and Phase 3
  2. Step 1 references map-codebase.md workflow
  3. Step 2 references brownfield-flow.md workflow with inline execution note
  4. Step 3 has brownfield PROJECT.md writing logic
  5. Step 4 explicitly states skip to Phase 5
  6. Guard instruction exists before Phase 3: "If MODE == brownfield: Skip to Phase 5"
  7. Phase 3 content is unchanged (guard is BEFORE Phase 3, not replacing it)
  </verify>
  <done>Phase 2B contains 4 steps (map-codebase, brownfield-flow, PROJECT.md, jump to Phase 5). Guard prevents brownfield from falling into greenfield phases. Phase 3 content unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add brownfield skip guards to Phases 6-8 and adapt Phase 10 Done banner</name>
  <files>commands/gsd/new-project.md</files>
  <action>
  Make three targeted additions to the later phases of `commands/gsd/new-project.md`:

  **A. Add brownfield guard to Phase 6 (Research Decision):**

  At the very beginning of Phase 6 (before the existing AskUserQuestion), add:

  ```
  **If MODE == "brownfield":** Skip this phase. Brownfield analysis serves as domain research. Continue to Phase 10 (Done).
  ```

  Do NOT modify any of the existing Phase 6 content. The guard is prepended, not replacing.

  **B. Add brownfield guard to Phase 7 (Define Requirements):**

  At the very beginning of Phase 7, add:

  ```
  **If MODE == "brownfield":** Skip this phase. brownfield-flow already produced REQUIREMENTS.md. Continue to Phase 10 (Done).
  ```

  Do NOT modify any of the existing Phase 7 content.

  **C. Add brownfield guard to Phase 8 (Create Roadmap):**

  At the very beginning of Phase 8, add:

  ```
  **If MODE == "brownfield":** Skip this phase. brownfield-flow already produced ROADMAP.md. Continue to Phase 10 (Done).
  ```

  Do NOT modify any of the existing Phase 8 content.

  **D. Adapt Phase 10 Done banner for dynamic artifact display:**

  Modify the Phase 10 artifact table to be dynamic. Replace the static table with conditional rows:

  ```
  | Artifact       | Location                    |
  |----------------|-----------------------------|
  | Project        | `.planning/PROJECT.md`      |
  | Config         | `.planning/config.json`     |
  {If .planning/brownfield-analysis.md exists:}
  | Analysis       | `.planning/brownfield-analysis.md` |
  {If .planning/research/ exists:}
  | Research       | `.planning/research/`       |
  | Requirements   | `.planning/REQUIREMENTS.md` |
  | Roadmap        | `.planning/ROADMAP.md`      |
  ```

  Also update the "Next Up" section to work for both paths:
  - Both paths suggest `/gsd:discuss-phase 1` as next step
  - Brownfield path notes: "Brownfield analysis and purpose routing complete."

  CRITICAL: All Phase 6-8 modifications are GUARD PREPENDS ONLY. The existing content of these phases must remain untouched for greenfield flow.
  </action>
  <verify>
  Read commands/gsd/new-project.md and verify:
  1. Phase 6 starts with brownfield skip guard, followed by unchanged existing content
  2. Phase 7 starts with brownfield skip guard, followed by unchanged existing content
  3. Phase 8 starts with brownfield skip guard, followed by unchanged existing content
  4. Phase 10 artifact table has conditional rows for Analysis and Research
  5. Greenfield content in Phases 6-8 is completely preserved
  6. No other phases were modified
  </verify>
  <done>Phases 6-8 have brownfield skip guards prepended. Phase 10 Done banner has dynamic artifact display. All greenfield content preserved unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Update execution_context, objective, and success_criteria for brownfield support</name>
  <files>commands/gsd/new-project.md</files>
  <action>
  Make minimal updates to the command's metadata sections to reflect brownfield capability:

  **A. Update objective description:**

  In the `<objective>` section, update the first line to mention brownfield:

  Current: "Initialize a new project through unified flow: questioning -> research (optional) -> requirements -> roadmap."

  New: "Initialize a new project through unified flow. Greenfield: questioning -> research (optional) -> requirements -> roadmap. Brownfield: map-codebase -> analysis -> purpose routing -> roadmap."

  Also update the "Creates:" list to include brownfield artifacts:
  - Add: `.planning/brownfield-analysis.md` -- codebase analysis (brownfield only)
  - Add: `.planning/codebase/` -- codebase documents (brownfield only)

  **B. Do NOT add brownfield references to execution_context:**

  Per research recommendation (conditional loading), do NOT add brownfield workflow/template references to the `<execution_context>` section. Brownfield references are loaded conditionally within Phase 2B instructions (via `@` references in the step descriptions). This keeps greenfield invocations lean.

  **C. Update success_criteria:**

  Add brownfield criteria to the `<success_criteria>` section at the end:
  - [ ] MODE detection completed (greenfield/scaffolded/brownfield)
  - [ ] If brownfield: codebase mapped, analysis complete, purpose routing done
  - [ ] If brownfield: brownfield PROJECT.md with Validated requirements committed
  - [ ] If brownfield: ROADMAP.md, STATE.md, REQUIREMENTS.md produced by brownfield-flow

  Keep all existing greenfield criteria unchanged.

  **D. Update output section:**

  Add brownfield-specific outputs to the `<output>` section:
  - `.planning/brownfield-analysis.md` (if brownfield)
  - `.planning/codebase/` (if brownfield)
  - `.planning/debug/` (if brownfield fix mode with debug bridge)
  </action>
  <verify>
  Read commands/gsd/new-project.md and verify:
  1. Objective mentions both greenfield and brownfield flows
  2. execution_context is UNCHANGED (no brownfield references added)
  3. success_criteria has brownfield items added after existing greenfield items
  4. output section lists brownfield-specific artifacts
  5. Creates list in objective includes brownfield analysis and codebase
  </verify>
  <done>Command metadata (objective, success_criteria, output) updated for brownfield. execution_context unchanged (conditional loading per research). All existing greenfield metadata preserved.</done>
</task>

</tasks>

<verification>
1. Read the complete modified new-project.md
2. Trace the greenfield path: Phase 1 (MODE=greenfield) -> Phase 2 (silent) -> Phase 3 (unchanged) -> Phase 4 (unchanged) -> Phase 5 (unchanged) -> Phase 6 (no guard triggered) -> Phase 7 (no guard triggered) -> Phase 8 (no guard triggered) -> Phase 10 (unchanged for greenfield)
3. Trace the brownfield path: Phase 1 (MODE=brownfield) -> Phase 2 (summary message) -> Phase 2B (map, analyze, PROJECT.md, jump) -> Phase 5 (unchanged) -> Phase 6 (guard: skip) -> Phase 7 (guard: skip) -> Phase 8 (guard: skip) -> Phase 10 (dynamic banner)
4. Verify greenfield Phases 3-8 content is unchanged (STATE-03)
5. Verify Phase 2B references brownfield-flow.md and map-codebase.md workflows
6. Verify no brownfield references in execution_context
</verification>

<success_criteria>
- Phase 2B brownfield pipeline runs map-codebase + brownfield-flow + PROJECT.md inline
- Brownfield path converges at Phase 5, skips Phases 6-8
- Phase 10 Done banner adapts to brownfield/greenfield artifacts
- Greenfield path through Phases 3-10 is completely unchanged (STATE-03)
- execution_context uses conditional loading (no brownfield references upfront)
- Single-command UX: no exit-and-return pattern for brownfield
</success_criteria>

<output>
After completion, create `.planning/phases/04-command-integration/04-02-SUMMARY.md`
</output>
