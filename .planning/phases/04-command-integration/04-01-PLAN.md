---
phase: 04-command-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/new-project.md
autonomous: true

must_haves:
  truths:
    - "Empty directory produces MODE=greenfield and reaches Phase 3 unchanged"
    - "Scaffolded project (<=10 files, <=3 commits) produces MODE=scaffolded and reaches Phase 3 with info message"
    - "Mature codebase (>10 files OR package+>10 commits) produces MODE=brownfield"
    - "MODE variable is the single source of truth for routing (raw signals not re-checked)"
    - "Greenfield Phase 3+ is byte-identical to pre-modification state"
  artifacts:
    - path: "commands/gsd/new-project.md"
      provides: "Detection upgrade in Phase 1 Step 3 + MODE-based routing in Phase 2"
      contains: "MODE=greenfield"
  key_links:
    - from: "Phase 1 Step 3 detection script"
      to: "Phase 2 MODE routing"
      via: "MODE variable (greenfield|scaffolded|brownfield)"
      pattern: "MODE=="
---

<objective>
Replace the basic 3-variable detection in Phase 1 Step 3 with the full brownfield-detection.md multi-signal script, and replace the binary Phase 2 "Brownfield Offer" with MODE-based three-way routing.

Purpose: This establishes the detection foundation that Phase 4 Plan 02 builds upon. The MODE variable (greenfield/scaffolded/brownfield) becomes the single routing decision point, replacing the current ad-hoc variable checks.

Output: Modified `commands/gsd/new-project.md` with upgraded detection and routing. Greenfield flow unchanged. Brownfield routing stub ready for Plan 02 to fill.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-command-integration/04-RESEARCH.md
@get-shit-done/references/brownfield-detection.md
@commands/gsd/new-project.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Phase 1 Step 3 detection with brownfield-detection.md script</name>
  <files>commands/gsd/new-project.md</files>
  <action>
  In `commands/gsd/new-project.md`, locate Phase 1 Step 3 "Detect existing code (brownfield detection)" (currently lines 60-65). Replace the 3-line Bash block with the complete 55-line multi-signal detection script from `get-shit-done/references/brownfield-detection.md` (the `<complete_bash_script>` section, lines 312-426).

  Specifically replace:
  ```bash
  CODE_FILES=$(find . -name "*.ts" -o -name "*.js" -o -name "*.py" -o -name "*.go" -o -name "*.rs" -o -name "*.swift" -o -name "*.java" 2>/dev/null | grep -v node_modules | grep -v .git | head -20)
  HAS_PACKAGE=$([ -f package.json ] || [ -f requirements.txt ] || [ -f Cargo.toml ] || [ -f go.mod ] || [ -f Package.swift ] && echo "yes")
  HAS_CODEBASE_MAP=$([ -d .planning/codebase ] && echo "yes")
  ```

  With the full brownfield-detection.md script that includes:
  - Signal 1: Code files (33 extensions, 12 exclusion directories)
  - Signal 2: Package manager (17 manifest files)
  - Signal 3: Git history depth
  - Signal 4: Directory structure complexity
  - Signal 5: Codebase map existence
  - Language detection function
  - Mode determination tree (greenfield/scaffolded/brownfield)
  - 7 diagnostic variable output (MODE, CODE_FILE_COUNT, PRIMARY_LANG, HAS_PACKAGE, GIT_COMMIT_COUNT, SRC_DIR_COUNT, HAS_CODEBASE_MAP)

  Copy the script verbatim from brownfield-detection.md. Do NOT modify the script logic. The detection module has been designed and tested in Phase 1.

  Keep the surrounding text unchanged:
  - The step header ("3. **Detect existing code (brownfield detection):**") stays
  - The instruction line after the Bash block ("**You MUST run all bash commands above using the Bash tool before proceeding.**") stays

  Important: Remove the `#!/usr/bin/env bash` shebang line when embedding -- the script runs inside a Bash tool call, not as a standalone script.
  </action>
  <verify>
  Read commands/gsd/new-project.md and verify:
  1. Phase 1 Step 3 contains the full detection script (search for "Signal 1: Code Files" and "Mode Determination")
  2. The script ends with diagnostic output lines (echo "MODE=$MODE" through echo "HAS_CODEBASE_MAP=$HAS_CODEBASE_MAP")
  3. The 7 output variables are present: MODE, CODE_FILE_COUNT, PRIMARY_LANG, HAS_PACKAGE, GIT_COMMIT_COUNT, SRC_DIR_COUNT, HAS_CODEBASE_MAP
  4. Phase 1 Steps 1 and 2 are unchanged
  5. The instruction line "You MUST run all bash commands above" is preserved
  </verify>
  <done>Phase 1 Step 3 contains the complete brownfield-detection.md script with all 5 signals, language detection, and mode determination. All 7 diagnostic variables are output. Steps 1-2 are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Phase 2 "Brownfield Offer" with MODE-based three-way routing</name>
  <files>commands/gsd/new-project.md</files>
  <action>
  Replace the entire Phase 2 section (currently "## Phase 2: Brownfield Offer", lines 69-92) with a new MODE-based routing section. The replacement must:

  1. Change the heading to "## Phase 2: Mode Routing"

  2. Implement three-way routing based on the MODE variable from Phase 1:

  **If MODE == "greenfield":**
  No existing code detected. Continue directly to Phase 3 (Deep Questioning). No message shown to user.

  **If MODE == "scaffolded":**
  Display informational message to user:
  ```
  This appears to be a scaffolded project ({CODE_FILE_COUNT} files, {GIT_COMMIT_COUNT} commits). Treating as a new project.
  ```
  Continue to Phase 3 (Deep Questioning).

  **If MODE == "brownfield":**
  Display detection summary to user:
  ```
  Existing codebase detected: {CODE_FILE_COUNT} files, {PRIMARY_LANG}, {GIT_COMMIT_COUNT} commits.
  ```
  Continue to Phase 2B (Brownfield Pipeline).

  3. Remove ALL references to:
  - AskUserQuestion with "Map codebase first" / "Skip mapping" options
  - "Exit command" / "Run `/gsd:map-codebase` first, then return"
  - Direct checks of CODE_FILES, HAS_PACKAGE, HAS_CODEBASE_MAP for routing (use MODE instead)

  4. Do NOT add Phase 2B content yet (Plan 02 handles that). Simply reference "Phase 2B" as the destination for brownfield routing. Add a placeholder comment: "<!-- Phase 2B: Brownfield Pipeline -- see below -->"

  5. Ensure Phase 3 heading and content remain COMPLETELY unchanged (do not modify anything from "## Phase 3: Deep Questioning" onward).

  Critical: The greenfield path must be the SILENT default. No detection messages, no brownfield mentions, no additional output for greenfield projects. The user experience for greenfield must be identical to pre-modification.
  </action>
  <verify>
  Read commands/gsd/new-project.md and verify:
  1. Phase 2 heading is "## Phase 2: Mode Routing"
  2. Three routing paths exist: greenfield (silent, to Phase 3), scaffolded (info message, to Phase 3), brownfield (summary, to Phase 2B)
  3. No AskUserQuestion in Phase 2 (the old "Map codebase first" prompt is gone)
  4. No "exit command" or "/gsd:map-codebase first" instructions remain
  5. Phase 3 heading "## Phase 3: Deep Questioning" and all content after it is unchanged
  6. MODE variable is the sole routing criterion (not CODE_FILES or HAS_PACKAGE directly)
  </verify>
  <done>Phase 2 uses MODE-based three-way routing. Greenfield is silent, scaffolded shows info message, brownfield routes to Phase 2B. No exit-and-return pattern. Phase 3+ unchanged.</done>
</task>

</tasks>

<verification>
1. Read the modified new-project.md from start through Phase 3 heading
2. Confirm Phase 1 Step 3 has full detection script with MODE output
3. Confirm Phase 2 uses MODE routing with three paths
4. Confirm Phase 3 content is identical to original (greenfield unchanged -- STATE-03)
5. Confirm no references to old detection variables (CODE_FILES, HAS_PACKAGE) in Phase 2 routing logic
</verification>

<success_criteria>
- Phase 1 Step 3 contains the complete brownfield-detection.md 55-line script
- Phase 2 routes by MODE variable: greenfield->Phase 3 (silent), scaffolded->Phase 3 (info), brownfield->Phase 2B
- No exit-and-return pattern remains
- Phase 3 and all subsequent phases are untouched
- Greenfield regression scenarios G1-G5 produce correct MODE values
</success_criteria>

<output>
After completion, create `.planning/phases/04-command-integration/04-01-SUMMARY.md`
</output>
