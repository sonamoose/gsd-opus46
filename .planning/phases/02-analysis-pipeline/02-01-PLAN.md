---
phase: 02-analysis-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-brownfield-analyzer.md
autonomous: true

must_haves:
  truths:
    - "Concerns in brownfield-analysis.md are classified as critical/moderate/minor with explicit criteria"
    - "Concerns within each severity level are sorted by scope of impact"
    - "Agent filters findings by subdirectory path prefix when scope parameter is provided"
    - "Agent produces full analysis when no scope parameter is given (default behavior unchanged)"
    - "Scoped analysis with zero matching findings returns informative message instead of empty sections"
  artifacts:
    - path: "agents/gsd-brownfield-analyzer.md"
      provides: "Enhanced brownfield analyzer agent with severity classification and scoped analysis"
      contains: "classify_severity"
    - path: "agents/gsd-brownfield-analyzer.md"
      provides: "Scoped analysis filtering logic"
      contains: "apply_scope"
  key_links:
    - from: "agents/gsd-brownfield-analyzer.md (classify_severity step)"
      to: "get-shit-done/templates/brownfield-summary.md (Top Concerns + Detailed Concerns sections)"
      via: "Agent applies severity criteria when filling template sections"
      pattern: "critical.*moderate.*minor"
    - from: "agents/gsd-brownfield-analyzer.md (apply_scope step)"
      to: ".planning/brownfield-analysis.md output"
      via: "Agent filters all findings by scope path prefix before writing"
      pattern: "scope"
---

<objective>
Enhance the `gsd-brownfield-analyzer` agent with formalized severity classification criteria (ANALYSIS-03) and scoped subdirectory analysis logic (ANALYSIS-04).

Purpose: The existing agent synthesizes 7 codebase documents but lacks explicit criteria for classifying concern severity and has no mechanism for filtering analysis to a specific subdirectory. These enhancements make the analysis output actionable (ranked by impact) and flexible (scoped to areas of interest).

Output: Modified `agents/gsd-brownfield-analyzer.md` with two new process steps: `classify_severity` and `apply_scope`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/gsd-brownfield-analyzer.md
@get-shit-done/templates/brownfield-summary.md
@.planning/phases/02-analysis-pipeline/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add severity classification criteria to brownfield-analyzer agent</name>
  <files>agents/gsd-brownfield-analyzer.md</files>
  <action>
Add a new step `classify_severity` to the `<process>` section of `agents/gsd-brownfield-analyzer.md`, inserted BETWEEN the existing `read_all_documents` step and `assess_health` step. This step formalizes how the agent classifies concerns into severity levels.

The new step content:

```xml
<step name="classify_severity">
Classify each concern extracted from CONCERNS.md (and cross-referenced findings from other documents) into severity levels using these criteria:

**Critical** — Any of:
- Breaks end-user functionality (users cannot complete a core workflow)
- Security vulnerability with exploitable vector
- Data loss or corruption risk
- Blocks development progress on critical path

**Moderate** — Any of:
- Degrades user experience noticeably
- Slows development velocity
- Performance bottleneck under normal load
- Accumulating tech debt in core modules (>3 files affected)

**Minor** — Any of:
- Cosmetic or naming inconsistency
- Style deviation from established conventions
- Low-traffic or edge-case performance issue
- Tech debt in peripheral code (1-2 files affected)

**Sorting within each severity level:**
1. Primary: Scope of impact — number of files/modules affected (more files = higher rank)
2. Secondary: Cross-document references — concerns that appear in multiple codebase documents rank higher than single-document findings

Apply these classifications when writing both the Top Concerns section (ranked list) and the Detailed Concerns section (grouped by severity) of the output.
</step>
```

Also update the `synthesize` step to reference the new classification: in the Top Concerns bullet, change "Extract from CONCERNS.md, ranked by impact" to "Extract from CONCERNS.md, classified using `classify_severity` criteria, ranked within severity by scope of impact."

Also add to the `<critical_rules>` section:

```
**CLASSIFY CONCERNS USING EXPLICIT SEVERITY CRITERIA.** Use the criteria defined in the `classify_severity` process step. Do not rely on intuition or vague "importance" — apply the specific conditions (breaks users = critical, degrades experience = moderate, cosmetic = minor). When a concern matches multiple severity levels, use the highest applicable level.
```

Also add to `<success_criteria>`:
```
- [ ] Every concern in Top Concerns has a severity tag matching classify_severity criteria
- [ ] Detailed Concerns section groups concerns by severity (critical first, then moderate, then minor)
- [ ] Within each severity group, concerns are sorted by scope of impact (files affected)
```
  </action>
  <verify>
Read `agents/gsd-brownfield-analyzer.md` and confirm:
1. A `classify_severity` step exists in `<process>` between `read_all_documents` and `assess_health`
2. The step defines exactly 3 severity levels: critical, moderate, minor
3. Each severity level has 4 bullet-point criteria
4. Sorting rules specify primary (file count) and secondary (cross-document references)
5. The `synthesize` step references `classify_severity`
6. `<critical_rules>` includes the severity classification rule
7. `<success_criteria>` includes 3 new severity-related checkboxes
  </verify>
  <done>
The brownfield-analyzer agent has explicit, deterministic severity classification criteria that any Claude instance can apply consistently. Critical/moderate/minor are defined by observable conditions (not subjective judgment), and sorting within severity is specified by scope of impact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add scoped analysis logic to brownfield-analyzer agent</name>
  <files>agents/gsd-brownfield-analyzer.md</files>
  <action>
Add a new step `apply_scope` to the `<process>` section of `agents/gsd-brownfield-analyzer.md`, inserted as the FIRST step (before `discover_documents`). This step handles the optional scope parameter that the workflow passes.

The new step content:

```xml
<step name="apply_scope" priority="first">
Check if a scope parameter was provided in the spawn prompt.

**If scope is provided** (a subdirectory path like `src/services/` or `app/api/`):
1. Store the scope path for filtering in subsequent steps
2. Read all 7 codebase documents as normal (do NOT skip any documents)
3. When extracting findings in `read_all_documents`, include ONLY findings where at least one mentioned file path starts with the scope prefix
4. For executive summary table dimensions with no in-scope findings, write: "No findings in scope: `{scope_path}`" with confidence "N/A"
5. For Top Concerns: only include concerns with at least one in-scope file path
6. For Detailed Concerns: only include concerns with in-scope file paths
7. Health rating: assess based on in-scope findings only
8. Add scope indicator to output header: `**Analysis Scope:** {scope_path}`

**If scope yields zero findings** across ALL documents:
Return early with this message instead of writing brownfield-analysis.md:
```
## Brownfield Analysis — Scope Empty

**Scope:** `{scope_path}`
**Result:** No findings in any codebase document mention paths within this scope.

**Suggestions:**
- Verify the path exists in the codebase
- Check if the path was covered during codebase mapping (`/gsd:map-codebase`)
- Try a broader scope (e.g., parent directory)
- Run without scope for full codebase analysis
```

**If no scope parameter** (default):
Proceed with full analysis. No filtering. This is the existing behavior — do not change it.
</step>
```

Also update the `<role>` section to mention scope capability. After the line "Your job: Read all documents, synthesize findings using the brownfield-summary template, write `.planning/brownfield-analysis.md`, return executive summary confirmation only." add:

"You accept an optional `scope` parameter (subdirectory path) to filter analysis to a specific area of the codebase."

Also update the `return_confirmation` step format to include scope when present. After the `**Documents analyzed:** {N} of 7` line, add:
`**Scope:** {scope_path or "full codebase"}`

Also add to `<critical_rules>`:

```
**RESPECT SCOPE PARAMETER WHEN PROVIDED.** If a scope path is given, filter ALL findings to only those mentioning file paths within the scope. Still read all 7 documents (do not skip documents), but only extract findings with in-scope file paths. When no findings match the scope, return the scope-empty message — do NOT write an empty analysis file.
```

Also add to `<success_criteria>`:
```
- [ ] If scope provided: only in-scope findings appear in output
- [ ] If scope provided: executive summary dimensions with no in-scope findings show "No findings in scope"
- [ ] If scope yields zero total findings: scope-empty message returned instead of empty file
- [ ] If no scope: full analysis produced (existing behavior unchanged)
```
  </action>
  <verify>
Read `agents/gsd-brownfield-analyzer.md` and confirm:
1. An `apply_scope` step exists as the first step in `<process>` with `priority="first"`
2. The step describes filtering behavior for when scope is provided
3. The step describes zero-findings handling with a specific message format
4. The step explicitly states default behavior (no scope = full analysis, unchanged)
5. The `<role>` section mentions scope capability
6. The `return_confirmation` format includes scope line
7. `<critical_rules>` includes the scope respect rule
8. `<success_criteria>` includes 4 new scope-related checkboxes
9. No other existing steps were removed or broken
10. The agent still has all original steps: discover_documents, read_all_documents, assess_health, synthesize, write_output, return_confirmation
  </verify>
  <done>
The brownfield-analyzer agent accepts an optional scope parameter, filters all findings by subdirectory path prefix when scope is provided, handles the zero-findings edge case gracefully, and preserves existing full-analysis behavior as the default. The scope parameter is documented in the role, confirmation format, critical rules, and success criteria.
  </done>
</task>

</tasks>

<verification>
Read `agents/gsd-brownfield-analyzer.md` end-to-end and verify:
1. Process steps are in order: apply_scope -> discover_documents -> read_all_documents -> classify_severity -> assess_health -> synthesize -> write_output -> return_confirmation
2. All original Phase 1 content is preserved (role, why_this_matters, philosophy, critical_rules, success_criteria sections all still present)
3. New severity classification has 3 levels with 4 criteria each
4. New scope logic handles 3 cases: scope with findings, scope with no findings, no scope
5. Agent definition is self-contained — no broken references to external files that do not exist
6. Total line count is approximately 280-320 lines (was 205 lines, adding ~75-115 lines)
</verification>

<success_criteria>
- agents/gsd-brownfield-analyzer.md has classify_severity step with explicit critical/moderate/minor criteria
- agents/gsd-brownfield-analyzer.md has apply_scope step as first process step
- All existing agent functionality preserved (no regressions)
- Severity sorting is deterministic: by scope of impact, then by cross-document references
- Scope filtering covers 3 cases: scoped with findings, scoped with no findings, unscoped (default)
- Requirements addressed: ANALYSIS-03 (severity classification), ANALYSIS-04 (scoped analysis — agent side)
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-pipeline/02-01-SUMMARY.md`
</output>
