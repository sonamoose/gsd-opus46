---
phase: 02-analysis-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/brownfield-flow.md
autonomous: true

must_haves:
  truths:
    - "Workflow checks for .planning/codebase/ prerequisite and errors if missing"
    - "Workflow spawns gsd-brownfield-analyzer agent via Task() to perform analysis"
    - "Workflow reads executive summary from brownfield-analysis.md and presents it inline to user"
    - "User sees a 10-line dashboard with health rating, 7-dimension table, and top concerns"
    - "Workflow accepts optional scope parameter and passes it to the analyzer agent"
    - "Workflow returns structured result with health, top_concerns, and summary_path"
    - "Workflow does NOT read .planning/codebase/*.md directly — only the agent does"
    - "Workflow does NOT write STATE.md or any file other than delegating to the agent"
  artifacts:
    - path: "get-shit-done/workflows/brownfield-flow.md"
      provides: "Analysis pipeline orchestration workflow"
      contains: "gsd-brownfield-analyzer"
      min_lines: 120
  key_links:
    - from: "get-shit-done/workflows/brownfield-flow.md (run_analysis step)"
      to: "agents/gsd-brownfield-analyzer.md"
      via: "Task() spawn with subagent_type"
      pattern: "subagent_type.*gsd-brownfield-analyzer"
    - from: "get-shit-done/workflows/brownfield-flow.md (present_dashboard step)"
      to: ".planning/brownfield-analysis.md"
      via: "Read executive summary section from agent output file"
      pattern: "brownfield-analysis.md"
    - from: "get-shit-done/workflows/brownfield-flow.md (determine_scope step)"
      to: "agents/gsd-brownfield-analyzer.md (apply_scope step)"
      via: "Scope parameter passed in Task() prompt"
      pattern: "scope"
---

<objective>
Create the `brownfield-flow.md` workflow that orchestrates the brownfield analysis pipeline: checks prerequisites, determines scope, spawns the analyzer agent, presents a user-facing dashboard, and returns structured results.

Purpose: This workflow is the entry point for brownfield codebase analysis. It bridges the gap between raw codebase documents (created by map-codebase) and actionable analysis (consumed by the user and purpose-routing in Phase 3). Without this workflow, the analyzer agent has no orchestrator and the user has no way to trigger or view analysis.

Output: New `get-shit-done/workflows/brownfield-flow.md` with 5 steps following the map-codebase.md workflow pattern.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/workflows/map-codebase.md
@agents/gsd-brownfield-analyzer.md
@get-shit-done/templates/brownfield-summary.md
@.planning/phases/02-analysis-pipeline/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brownfield-flow.md with prerequisite check, scope determination, and agent spawning</name>
  <files>get-shit-done/workflows/brownfield-flow.md</files>
  <action>
Create `get-shit-done/workflows/brownfield-flow.md` following the exact structural pattern of `get-shit-done/workflows/map-codebase.md`: a `<purpose>` section, a `<philosophy>` section, a `<process>` section with named steps, and a `<success_criteria>` section.

The file structure:

```markdown
<purpose>
Orchestrate brownfield codebase analysis by spawning gsd-brownfield-analyzer agent and presenting results.

The workflow reads NO codebase documents directly. It delegates all analysis work to the agent, receives a confirmation, reads the agent's output file for presentation, and returns a structured result.

Input: .planning/codebase/ (7 documents created by map-codebase workflow)
Output: .planning/brownfield-analysis.md (written by agent) + inline dashboard for user
</purpose>

<philosophy>
**Orchestrator stays lean:**
This workflow does NOT read .planning/codebase/*.md documents. The agent reads them. The workflow only reads .planning/brownfield-analysis.md (the agent's output) for presentation.

**Stateless:**
This workflow does NOT write STATE.md, PROJECT.md, or any planning file. It only orchestrates the agent and presents results. State updates happen when new-project.md integrates this workflow in Phase 4.

**Analysis only (Phase 2):**
This workflow performs analysis and returns results. It does NOT ask users questions about purpose (fix/improve/refactor). Purpose routing is Phase 3, which will EXTEND this file with additional steps.

**Follow map-codebase pattern:**
Spawn agent via Task(), agent writes file, agent returns lightweight confirmation, workflow presents results to user.
</philosophy>
```

Then write the `<process>` section with the first 3 steps:

**Step 1: check_prerequisites**
```xml
<step name="check_prerequisites">
Verify that codebase mapping has been completed:

```bash
ls .planning/codebase/*.md 2>/dev/null | wc -l
```

**If 0 files found:**
```
Codebase documents not found. The brownfield analyzer needs .planning/codebase/*.md documents as input.

Run `/gsd:map-codebase` first to map the codebase, then re-run this workflow.
```
Stop workflow. Do not proceed.

**If 1-6 files found:**
Note which documents are available. Proceed with partial analysis. The agent handles missing documents gracefully (marks dimensions as "Not available").

**If 7 files found:**
All documents available. Proceed normally.

Continue to determine_scope.
</step>
```

**Step 2: determine_scope**
```xml
<step name="determine_scope">
Check if a scope parameter was provided in the workflow invocation.

The scope parameter is an optional subdirectory path (e.g., `src/services/`, `app/api/`) passed by the caller (future new-project.md or manual invocation).

**If scope is provided:**
Store the scope path. It will be passed to the agent in the next step.
Validate the scope path is a plausible directory (contains `/` or is a top-level directory name).

**If no scope is provided:**
Default to full codebase analysis. No filtering.

Do NOT ask the user for scope. The caller decides whether to pass scope. This keeps the workflow non-interactive.

Continue to run_analysis.
</step>
```

**Step 3: run_analysis**
```xml
<step name="run_analysis">
Spawn the brownfield-analyzer agent to synthesize codebase documents.

Use Task tool with:
  subagent_type: "gsd-brownfield-analyzer"
  description: "Analyze codebase documents and write brownfield summary"

Prompt (pass to agent):
```
First, read your agent definition for full instructions:
@agents/gsd-brownfield-analyzer.md

{If scope is provided:}
Scope: {scope_path}
Only include findings with file paths starting with `{scope_path}`.

{If no scope:}
Scope: full codebase (no filtering)

Analyze the .planning/codebase/*.md documents.
Write .planning/brownfield-analysis.md using the brownfield-summary template.
Return executive summary confirmation only (~20 lines).
```

Wait for the agent to complete.
Read the agent's return value for the executive summary confirmation (health rating, summary table, top concerns).

Continue to present_dashboard.
</step>
```
  </action>
  <verify>
Read `get-shit-done/workflows/brownfield-flow.md` and confirm:
1. File has `<purpose>`, `<philosophy>`, and `<process>` sections
2. `check_prerequisites` step verifies .planning/codebase/*.md exists
3. `check_prerequisites` stops workflow if 0 files found
4. `determine_scope` handles both scope-provided and no-scope cases
5. `determine_scope` does NOT use AskUserQuestion or any interactive prompt
6. `run_analysis` spawns agent with subagent_type "gsd-brownfield-analyzer"
7. `run_analysis` passes scope parameter to agent when provided
8. Workflow does NOT contain any Read calls to .planning/codebase/*.md
  </verify>
  <done>
The brownfield-flow workflow has its first 3 steps: prerequisite checking, scope determination, and agent spawning. The workflow follows the map-codebase pattern of delegating to an agent via Task(). The scope parameter is passed through to the agent without interactive prompting.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard presentation and structured result return to brownfield-flow.md</name>
  <files>get-shit-done/workflows/brownfield-flow.md</files>
  <action>
Add steps 4 and 5 to the `<process>` section of `get-shit-done/workflows/brownfield-flow.md`, plus the `<success_criteria>` section.

**Step 4: present_dashboard**
```xml
<step name="present_dashboard">
Read the executive summary from the agent's output file and present it inline to the user.

Read `.planning/brownfield-analysis.md` — extract the content from the beginning through the end of the "Top Concerns" section (approximately the first 30-40 lines, stopping before the "Architecture Overview" section).

Present to the user inline (this IS the 10-line dashboard from ANALYSIS-02):

```
## Codebase Analysis Complete

**Health:** {health_rating from document header}
{If scoped: **Scope:** `{scope_path}`}

| Dimension | Finding | Confidence |
|-----------|---------|------------|
{7 rows from Executive Summary table}

**Top Concerns:**
{numbered list of top 3-5 concerns with severity tags}

---
Full analysis: `.planning/brownfield-analysis.md`
```

**Important:** Read only the executive summary + top concerns sections from the file. Do NOT read or display the detailed sections (Architecture Overview, Technology Stack, etc.). Those are Tier 2 reference material the user can consult in the file.

Continue to return_result.
</step>
```

**Step 5: return_result**
```xml
<step name="return_result">
Return a structured result to the caller (future new-project.md or manual invocation).

```
## Analysis Pipeline Complete

**Output:** `.planning/brownfield-analysis.md`
**Health:** {health_rating}
**Scope:** {scope_path or "full codebase"}
**Top concern:** {first concern title and severity}

Ready for purpose routing (Phase 3).
```

This lightweight return enables the caller to decide next steps based on health rating and top concerns without re-reading the analysis file.

End workflow.
</step>
```

Then add the `<success_criteria>` section at the end of the file:

```xml
<success_criteria>
- .planning/codebase/*.md documents verified (prerequisite check)
- gsd-brownfield-analyzer agent spawned via Task() with optional scope parameter
- Agent writes .planning/brownfield-analysis.md (workflow does NOT write this file)
- Executive summary (health + 7-dimension table + top concerns) presented inline to user
- Inline dashboard is approximately 10 lines of content (table rows + health + concerns header)
- Structured result returned to caller with health, scope, and top concern
- Workflow did NOT read .planning/codebase/*.md directly
- Workflow did NOT write STATE.md or any file besides delegating to agent
- Workflow did NOT ask user interactive questions (analysis only, no purpose routing)
</success_criteria>
```
  </action>
  <verify>
Read `get-shit-done/workflows/brownfield-flow.md` end-to-end and confirm:
1. File has complete structure: purpose, philosophy, process (5 steps), success_criteria
2. `present_dashboard` step reads .planning/brownfield-analysis.md (NOT .planning/codebase/*.md)
3. `present_dashboard` extracts executive summary + top concerns only (not detailed sections)
4. The inline presentation format matches the 10-line dashboard requirement (health + table + concerns)
5. `return_result` step returns lightweight structured result
6. `success_criteria` section has 9 items matching the must_haves
7. Total file length is approximately 120-180 lines
8. The workflow references `gsd-brownfield-analyzer` by name (subagent_type)
9. No steps reference Phase 3 concepts (purpose selection, questioning, routing)
10. The workflow is analysis-only: no interactive prompts, no state writes
  </verify>
  <done>
The brownfield-flow workflow is complete with all 5 steps: check_prerequisites, determine_scope, run_analysis, present_dashboard, return_result. It follows the map-codebase pattern, presents a user-facing dashboard, and returns structured results. The workflow is analysis-only (Phase 2 scope) with no purpose routing (Phase 3 extension point).
  </done>
</task>

</tasks>

<verification>
1. Read `get-shit-done/workflows/brownfield-flow.md` end-to-end
2. Verify it follows map-codebase.md structural pattern (purpose, philosophy, process with named steps, success_criteria)
3. Verify NO references to .planning/codebase/*.md in any Read/Glob operations (only the agent reads those)
4. Verify the agent is spawned with subagent_type "gsd-brownfield-analyzer" (not Explore or generic)
5. Verify scope parameter is passed through to agent when provided
6. Verify the dashboard presentation reads from .planning/brownfield-analysis.md (agent output)
7. Verify no interactive prompts (AskUserQuestion, user input requests)
8. Verify no state file writes (STATE.md, PROJECT.md)
9. Verify no Phase 3 content (purpose selection, questioning, routing)
10. Total file length approximately 120-180 lines
</verification>

<success_criteria>
- get-shit-done/workflows/brownfield-flow.md exists with 5 process steps
- Workflow spawns gsd-brownfield-analyzer via Task() delegation
- Dashboard presentation extracts and displays executive summary inline (~10 lines)
- Optional scope parameter flows through workflow to agent
- Workflow is stateless (no file writes except agent delegation)
- Workflow is non-interactive (no user prompts)
- Workflow is analysis-only (no Phase 3 purpose routing)
- Requirements addressed: INFRA-02 (workflow creation), ANALYSIS-01 (4-aspect via agent), ANALYSIS-02 (dashboard presentation), ANALYSIS-04 (scope parameter — workflow side)
</success_criteria>

<output>
After completion, create `.planning/phases/02-analysis-pipeline/02-02-SUMMARY.md`
</output>
