---
phase: 05-state-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/templates/project.md
  - get-shit-done/templates/state.md
autonomous: true

must_haves:
  truths:
    - "project.md template defines a Codebase Context section for brownfield projects"
    - "state.md template defines a Codebase Context subsection under Accumulated Context"
    - "Brownfield sections include conditional markers so greenfield projects omit them"
    - "project.md brownfield guidance instructs inferring Validated requirements from ARCHITECTURE.md and STACK.md"
  artifacts:
    - path: "get-shit-done/templates/project.md"
      provides: "Codebase Context section in template, expanded brownfield guidance, updated guidelines"
      contains: "## Codebase Context"
    - path: "get-shit-done/templates/state.md"
      provides: "Codebase Context subsection in template, updated sections docs, updated lifecycle"
      contains: "### Codebase Context"
  key_links:
    - from: "get-shit-done/templates/project.md"
      to: "commands/gsd/new-project.md Phase 2B Step 3"
      via: "Template guides writing of brownfield PROJECT.md"
      pattern: "Codebase Context"
    - from: "get-shit-done/templates/state.md"
      to: "get-shit-done/workflows/brownfield-flow.md generate_roadmap"
      via: "Template guides roadmapper writing of STATE.md"
      pattern: "Codebase Context"
---

<objective>
Update project.md and state.md templates to formalize brownfield context sections that downstream agents (planner, executor) can reliably parse.

Purpose: Currently, brownfield-specific content (Codebase Mode, analysis reference, purpose) is written ad hoc by new-project.md Phase 2B Step 3 without template guidance. Formalizing these sections in templates ensures consistent structure across all brownfield projects and makes the content discoverable by downstream agents.

Output: Updated templates with Codebase Context sections, expanded brownfield guidance, and updated documentation sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-state-integration/05-RESEARCH.md
@get-shit-done/templates/project.md
@get-shit-done/templates/state.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Codebase Context section and expand brownfield guidance in project.md template</name>
  <files>get-shit-done/templates/project.md</files>
  <action>
  Three changes to get-shit-done/templates/project.md:

  **1. Add Codebase Context section to the template markdown (inside `<template>` block).**

  Insert the following section AFTER the `## Context` block (line ~49) and BEFORE `## Constraints` (line ~51):

  ```markdown
  ## Codebase Context

  <!-- For brownfield projects only. Omit entire section for greenfield. -->

  **Mode:** brownfield
  **Primary Language:** [Language from detection]
  **Analysis:** `.planning/brownfield-analysis.md`
  **Health:** [Good / Moderate / Concerning]
  **Purpose:** [Fix / Improve / Refactor]
  **Codebase Map:** `.planning/codebase/`
  ```

  **2. Expand the `<brownfield>` section (lines 146-167) with explicit field-writing instructions.**

  Replace the current `<brownfield>` content with expanded guidance that adds:
  - After step 2 (Infer Validated requirements), add specifics: "Read ARCHITECTURE.md for architecture patterns and key capabilities. Read STACK.md for language, framework, and runtime. Format each inferred capability as `- ✓ [Capability description] — existing` in the Validated section."
  - After step 4 (Initialize), add step 5: "**Populate Codebase Context section** — Fill Mode (always `brownfield`), Primary Language (from detection), Analysis (path to brownfield-analysis.md), Health (from analysis summary rating), Purpose (from user selection during brownfield-flow), Codebase Map (path to .planning/codebase/)."

  **3. Add Codebase Context entry to `<guidelines>` section (after the Context entry, before Constraints).**

  Add:
  ```
  **Codebase Context:**
  - Only present for brownfield projects; omit entirely for greenfield
  - Mode is always "brownfield" when this section exists
  - Primary Language from automatic detection during brownfield-flow
  - Analysis references .planning/brownfield-analysis.md for detailed findings
  - Health rating (Good/Moderate/Concerning) from analysis summary
  - Purpose (Fix/Improve/Refactor) from user selection
  - Codebase Map references .planning/codebase/ for full code documentation
  ```
  </action>
  <verify>
  Read the updated file and confirm:
  1. Template block contains `## Codebase Context` section between Context and Constraints
  2. Brownfield section has 5 steps including explicit Validated requirements inference instructions referencing ARCHITECTURE.md and STACK.md
  3. Guidelines section has a Codebase Context entry
  4. The comment "For brownfield projects only. Omit entire section for greenfield." is present
  </verify>
  <done>
  project.md template defines a structured Codebase Context section in its template markdown, the brownfield guidance explicitly instructs reading ARCHITECTURE.md and STACK.md to infer Validated requirements, and the guidelines document the new section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Codebase Context subsection and update docs in state.md template</name>
  <files>get-shit-done/templates/state.md</files>
  <action>
  Three changes to get-shit-done/templates/state.md:

  **1. Add Codebase Context subsection to the File Template (inside the markdown code block).**

  Insert the following subsection AFTER `### Blockers/Concerns` (line ~65) and BEFORE `## Session Continuity` (line ~69):

  ```markdown
  ### Codebase Context

  <!-- For brownfield projects only. Omit for greenfield. -->

  **Analysis:** `.planning/brownfield-analysis.md`
  **Purpose:** [Fix / Improve / Refactor]
  **Health:** [Good / Moderate / Concerning]
  ```

  This is 3 lines of content -- well within the 100-line size constraint.

  **2. Update `<sections>` documentation (lines 115-163).**

  Add a new documentation entry under the Accumulated Context section description (after Blockers/Concerns docs, around line 157), before `### Session Continuity`:

  ```
  **Codebase Context:** For brownfield projects only.
  - Analysis reference to .planning/brownfield-analysis.md
  - Purpose (Fix/Improve/Refactor) from user selection during brownfield-flow
  - Health rating from analysis summary
  - Enables downstream agents to quickly identify brownfield mode and link to detailed analysis
  - Omit entirely for greenfield projects
  ```

  **3. Update `<lifecycle>` section (lines 90-113).**

  In the Creation subsection (after "Set position to 'Phase 1 ready to plan'", line 95), add:
  ```
  - If brownfield: populate Codebase Context with analysis reference, purpose, and health from brownfield-flow results
  ```
  </action>
  <verify>
  Read the updated file and confirm:
  1. File Template code block contains `### Codebase Context` subsection between Blockers/Concerns and Session Continuity
  2. The `<sections>` block documents Codebase Context with brownfield-only marker
  3. The `<lifecycle>` Creation block mentions brownfield Codebase Context population
  4. Comment "For brownfield projects only. Omit for greenfield." is present in template
  </verify>
  <done>
  state.md template defines a Codebase Context subsection under Accumulated Context for brownfield projects, the sections documentation explains when and how it's populated, and the lifecycle includes brownfield initialization step.
  </done>
</task>

</tasks>

<verification>
1. Both template files parse correctly as Markdown
2. Template sections are properly nested (Codebase Context is inside template code blocks, not outside)
3. Brownfield conditional markers are present in both templates
4. No changes made to any agent files (gsd-planner.md, gsd-executor.md)
5. No changes made to any workflow files (brownfield-flow.md, execute-plan.md, execute-phase.md)
6. No changes made to command files (new-project.md)
</verification>

<success_criteria>
1. project.md template has a `## Codebase Context` section in its template block with Mode, Primary Language, Analysis, Health, Purpose, Codebase Map fields
2. project.md brownfield guidance explicitly references ARCHITECTURE.md and STACK.md for Validated requirements inference
3. state.md template has a `### Codebase Context` subsection under Accumulated Context with Analysis, Purpose, Health fields
4. Both templates mark brownfield sections as conditional ("omit for greenfield")
5. Documentation sections (guidelines, sections, lifecycle) are updated to reflect new content
</success_criteria>

<output>
After completion, create `.planning/phases/05-state-integration/05-01-SUMMARY.md`
</output>
