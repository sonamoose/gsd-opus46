---
phase: 03-purpose-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/references/brownfield-questioning.md
autonomous: true

must_haves:
  truths:
    - "Fix mode questioning starts from analysis concerns as selectable options, not freeform input"
    - "Improve mode questioning starts from user vision and constrains by analysis architecture findings"
    - "Refactor mode questioning captures both pain points and target state"
    - "All questioning follows 'thinking partner, not interviewer' philosophy from questioning.md"
    - "Question flows are adaptive references (available by category), not mandatory checklists"
    - "'Other' purpose has a minimal thread that keeps user in brownfield context"
  artifacts:
    - path: "get-shit-done/references/brownfield-questioning.md"
      provides: "Purpose-specific questioning patterns for brownfield fix/improve/refactor modes"
      contains: "fix_thread"
      min_lines: 120
  key_links:
    - from: "get-shit-done/references/brownfield-questioning.md (philosophy section)"
      to: "get-shit-done/references/questioning.md (philosophy)"
      via: "Adapts greenfield questioning philosophy for brownfield context — observed state vs imagined state"
      pattern: "thinking partner"
    - from: "get-shit-done/references/brownfield-questioning.md (fix_thread)"
      to: ".planning/brownfield-analysis.md (Top Concerns section)"
      via: "Fix mode references analysis concerns by severity as starting options"
      pattern: "concern.*severity"
    - from: "get-shit-done/references/brownfield-questioning.md (improve_thread)"
      to: ".planning/brownfield-analysis.md (Architecture Overview section)"
      via: "Improve mode references analysis architecture for fit assessment"
      pattern: "architecture"
---

<objective>
Create the `brownfield-questioning.md` reference file that defines purpose-specific questioning patterns for brownfield codebase workflows.

Purpose: After the user selects their purpose (fix/improve/refactor), the brownfield-flow workflow needs a reference for what questions to ask. This file provides the adaptive questioning patterns for each purpose mode, grounded in analysis findings rather than generic prompts. Without this reference, Step 7 of brownfield-flow.md has no questioning guide to load.

Output: New `get-shit-done/references/brownfield-questioning.md` reference file with philosophy section + three purpose threads (fix, improve, refactor) + other/custom thread.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/references/questioning.md
@get-shit-done/workflows/discuss-phase.md
@get-shit-done/templates/brownfield-summary.md
@.planning/phases/03-purpose-routing/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brownfield-questioning.md with philosophy and fix/improve/refactor question threads</name>
  <files>get-shit-done/references/brownfield-questioning.md</files>
  <action>
Create `get-shit-done/references/brownfield-questioning.md` as a new file. This reference file follows the structural pattern of `get-shit-done/references/questioning.md` but is specialized for brownfield purpose-specific questioning.

The file must contain these sections in order:

**1. Header and purpose statement:**
```markdown
# Brownfield Questioning Guide

Purpose-specific questioning patterns for brownfield codebase workflows. Used by `brownfield-flow.md` Step 7 (purpose_questioning) to guide adaptive conversation after the user selects their purpose.

**Key difference from greenfield questioning:**
- Greenfield: "What do you want to build?" (imagined state -> project definition)
- Brownfield: "What do you want to change?" (observed state -> desired state)

The analysis is your anchor. Every question should reference specific findings from brownfield-analysis.md.
```

**2. `<philosophy>` section:**
Adapt the greenfield questioning.md philosophy for brownfield context. Must include:
- "You are a diagnostic partner, not an interviewer." (adapted from "thinking partner")
- The analysis already identified what IS. Your job is to help the user articulate what they want to DO about it.
- Start from observed state, navigate toward action.
- Follow the thread: each answer informs the next question. Do NOT walk through all questions regardless of answers.
- These question flows are REFERENCES (available questions by category), not SCRIPTS (mandatory sequences). Skip irrelevant questions. Dive deeper where the user shows energy.

**3. `<fix_thread>` section — Fix Mode questioning:**
```markdown
## Fix Mode

Start from concerns. Help user prioritize and provide reproduction context.

**Context anchor:** Top Concerns from brownfield-analysis.md
**Goal:** Narrow scope to specific, actionable fixes

**Question flow:**

### 1. Scope Selection
Present top concerns from analysis as selectable options.
Use AskUserQuestion (multiSelect: true):
  header: "Focus"
  question: "The analysis found these concerns. Which do you want to address?"
  options: [top 3-5 concerns from analysis, each with severity tag]

If user selects none: Ask what issue they want to address instead (freeform).
If user selects all: Note that the roadmap will prioritize by severity.

### 2. Concern Validation (for each selected concern)
For each selected concern, ask 1-2 adaptive follow-ups:

a. Observation check:
   "Have you seen [concern description] in practice?"
   - "Yes — here's what happens: [gather reproduction details]"
   - "Yes, but I can't reliably reproduce it"
   - "No — the analysis flagged it but I haven't noticed it"

   If yes with details: Capture reproduction steps. These feed into debug bridging (Step 8).
   If no: Note as analysis-only finding. Lower priority unless severity is critical.

b. Impact from user perspective (may differ from analysis severity):
   "How does this affect you?"
   - "Blocks users / breaks functionality"
   - "Degrades experience but workarounds exist"
   - "Slows development / causes confusion"
   - "Minor annoyance"

### 3. Priority Confirmation
After validating all selected concerns:
  "Based on your input, here's the priority order: [re-ranked list].
   Does this look right, or should anything move up/down?"

### 4. Scope Decision
  "Fix root causes or patch symptoms first?"
  - "Root cause — proper fix even if it takes longer"
  - "Quick patch — stop the bleeding, fix properly later"
  - "Mix — root cause for critical, patch for others"

### 5. Decision Gate
  "Ready to create a fix roadmap based on these priorities?"
  - "Yes, create roadmap"
  - "I want to debug the top issue first" (routes to Step 8: bridge_to_debug)
  - "Let me add more context"
```

**4. `<improve_thread>` section — Improve Mode questioning:**
```markdown
## Improve Mode

Start from user's vision. Constrain by analysis findings.

**Context anchor:** Architecture + Stack from brownfield-analysis.md
**Goal:** Define improvements that fit existing architecture

**Question flow:**

### 1. Vision
  "What do you want this codebase to do that it doesn't do now?"
  Freeform response. Then follow-up probing (like greenfield questioning):
  - "Walk me through using that."
  - "What does that actually look like?"
  - "Who benefits from this?"

### 2. Architecture Fit
  Present analysis architecture context:
  "Your codebase uses [architecture pattern from analysis]. Your improvement would
  [extend existing patterns / require new patterns / need evaluation]."

  Ask: "Does this fit within the current architecture, or are you open to changes?"

### 3. Affected Areas
  Cross-reference improvement scope with analysis findings:
  "This would touch [areas from analysis]. The analysis noted these concerns in those areas: [relevant concerns]."

  Ask: "Are you aware of these? Should we address them as part of the improvement?"

### 4. Constraints
  Adapted from analysis context:
  - "Test coverage in affected areas is [N% from analysis]. Maintain or improve?"
  - "Any performance targets for the new capability?"
  - "Timeline or deadline considerations?"

  Skip constraints that don't apply. Don't ask about test coverage if the codebase has no tests.

### 5. Decision Gate
  "Ready to create an improvement roadmap?"
  - "Yes, create roadmap"
  - "Let me refine the scope"
```

**5. `<refactor_thread>` section — Refactor Mode questioning:**
```markdown
## Refactor Mode

Start from pain points. Navigate toward target architecture.

**Context anchor:** Concerns + Conventions from brownfield-analysis.md
**Goal:** Define target state and safe transformation path

**Question flow:**

### 1. Pain Point Selection
  Present moderate/minor concerns + structural findings as pain point options.
  Use AskUserQuestion (multiSelect: true):
    header: "Pain Points"
    question: "Which areas cause the most development pain?"
    options: [concerns + structural issues from analysis]

  Also ask: "Any pain points not captured in the analysis?"

### 2. Target State
  For each major pain point area, ask about the desired end state:

  a. Architecture:
     "Keep current [pattern from analysis] or move toward something else?"
     If moving: "What pattern? [suggest based on codebase size and concerns]"

  b. Conventions:
     "The analysis found [convention patterns]. Standardize on these or introduce new ones?"

  c. Testing:
     "Current test coverage is [from analysis]. What's your target?"
     Skip if user hasn't mentioned testing as a pain point.

### 3. Risk Tolerance
  "How much disruption is acceptable?"
  - "Small, safe changes — incremental improvement"
  - "Moderate restructuring — some temporary breakage OK"
  - "Significant rewrite — willing to rebuild sections"

### 4. Strategy
  Based on risk tolerance, confirm approach:
  - Low risk: "We'll focus on quick wins — naming, conventions, small patterns"
  - Moderate: "We'll restructure modules and clean up architecture"
  - High risk: "We'll plan a phased rewrite of the most painful areas"

### 5. Decision Gate
  "Ready to create a refactoring roadmap?"
  - "Yes, create roadmap"
  - "Let me reconsider the scope"
```

**6. `<other_thread>` section — Custom purpose questioning:**
```markdown
## Other / Custom Purpose

Minimal thread for goals that don't fit fix/improve/refactor categories.

**Context anchor:** Full brownfield-analysis.md
**Goal:** Understand user's goal and connect to analysis findings

**Question flow:**

### 1. Goal
  "What's your goal for this codebase?"
  Freeform response.

### 2. Analysis Relevance
  "How does the existing codebase relate to this goal?"
  - "It's the foundation I'm building on"
  - "It needs changes to support my goal"
  - "I'm trying to understand it better"
  - "Other — let me explain"

### 3. Relevant Findings
  "Which findings from the analysis are relevant to your goal?"
  Present top findings, let user select or describe.

### 4. Decision Gate
  "Ready to create a roadmap for this goal?"
  - "Yes, create roadmap"
  - "Let me explain more"
```

**7. `<anti_patterns>` section:**
```markdown
## Anti-Patterns

- **Checklist walking** — Going through all questions regardless of what the user said. If they selected one critical concern, don't ask about conventions and testing targets.
- **Ignoring analysis context** — Asking "What's wrong?" when the analysis already identified 5 concerns. Start from findings, not from zero.
- **Generic questions** — "What do you want to do?" is bad. "The analysis found 2 critical issues in your auth system. Want to fix those first?" is good.
- **Premature roadmapping** — Jumping to roadmap creation before understanding priorities and constraints. The questioning phase exists to gather context the analysis couldn't.
- **Over-questioning** — If user has clear priorities and just wants to proceed, let them. 2-3 questions may be enough. Don't pad to fill a template.
- **Scope creep across modes** — Fix mode should not drift into refactoring discussions. If the user starts talking about restructuring, suggest switching to refactor mode.
```

The complete file should be approximately 150-200 lines. Ensure all AskUserQuestion examples use proper header/question/options format consistent with GSD patterns.
  </action>
  <verify>
Read `get-shit-done/references/brownfield-questioning.md` and confirm:
1. File has header, philosophy, fix_thread, improve_thread, refactor_thread, other_thread, anti_patterns sections
2. Philosophy references "diagnostic partner, not interviewer"
3. Fix thread starts from analysis concerns as selectable options (not freeform)
4. Improve thread starts from user vision (freeform), then constrains by analysis
5. Refactor thread captures both pain points (from analysis) AND target state
6. Other thread is minimal (3-4 questions) and keeps user in brownfield context
7. All threads have a "Decision Gate" as final step
8. Anti-patterns section includes "checklist walking" and "ignoring analysis context"
9. Question flows use AskUserQuestion format where appropriate
10. No thread mandates walking through all questions — adaptive, not sequential
  </verify>
  <done>
The brownfield-questioning.md reference file exists with purpose-specific questioning patterns for all four modes (fix, improve, refactor, other). Each thread is grounded in analysis findings, follows adaptive questioning philosophy, and ends with a decision gate. The file serves as the questioning reference that brownfield-flow.md Step 7 loads.
  </done>
</task>

</tasks>

<verification>
1. Read `get-shit-done/references/brownfield-questioning.md` end-to-end
2. Verify it follows the structural pattern of `get-shit-done/references/questioning.md` (philosophy + question types/threads + anti-patterns)
3. Verify fix_thread references analysis concerns by severity (not generic "what's wrong?")
4. Verify improve_thread starts with vision and constrains by analysis architecture
5. Verify refactor_thread includes target state questions (not just pain identification)
6. Verify all AskUserQuestion examples use consistent format (header, question, options)
7. Verify philosophy explicitly states these are REFERENCES, not SCRIPTS
8. Verify no question thread exceeds 5 steps (keep each thread focused)
9. Verify file length is approximately 150-200 lines
</verification>

<success_criteria>
- get-shit-done/references/brownfield-questioning.md exists with 4 purpose threads
- Fix thread starts from analysis concerns as selectable options
- Improve thread starts from user vision, constrains by analysis findings
- Refactor thread captures pain points AND target state
- Other thread is minimal and keeps user in brownfield context
- All threads follow adaptive questioning philosophy (not checklists)
- Requirement foundation: ROUTE-02 (purpose-specific questioning patterns)
</success_criteria>

<output>
After completion, create `.planning/phases/03-purpose-routing/03-01-SUMMARY.md`
</output>
