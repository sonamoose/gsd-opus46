---
phase: 03-purpose-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/templates/brownfield-roadmap.md
autonomous: true

must_haves:
  truths:
    - "Fix mode roadmap orders phases by severity (critical first, then moderate, then minor)"
    - "Improve mode roadmap orders phases by dependency (foundations first, dependent features later)"
    - "Refactor mode roadmap orders phases by impact/effort ratio (quick wins first)"
    - "Template includes purpose-specific requirement ID prefixes (CONCERN-, IMPROVE-, REFACTOR-)"
    - "Template includes coverage table mapping each item to a phase with severity/priority"
    - "Template provides ordering rationale for each phase explaining why it is sequenced where it is"
  artifacts:
    - path: "get-shit-done/templates/brownfield-roadmap.md"
      provides: "Purpose-aware roadmap template for brownfield projects with severity/dependency/ratio ordering"
      contains: "Ordering"
      min_lines: 100
  key_links:
    - from: "get-shit-done/templates/brownfield-roadmap.md (fix mode section)"
      to: ".planning/brownfield-analysis.md (Top Concerns section)"
      via: "Fix roadmap phases derive from concerns grouped by severity"
      pattern: "severity.*critical.*moderate.*minor"
    - from: "get-shit-done/templates/brownfield-roadmap.md (improve mode section)"
      to: ".planning/brownfield-analysis.md (Architecture Overview section)"
      via: "Improve roadmap phases order by architectural dependency"
      pattern: "dependency.*foundation"
    - from: "get-shit-done/templates/brownfield-roadmap.md"
      to: "agents/gsd-roadmapper.md"
      via: "Template guides roadmapper agent to produce purpose-aware roadmap structure"
      pattern: "roadmap"
---

<objective>
Create the `brownfield-roadmap.md` template that defines the purpose-aware roadmap structure for brownfield projects.

Purpose: Brownfield roadmaps differ fundamentally from greenfield roadmaps. Greenfield roadmaps order phases by dependency (build foundation first). Brownfield roadmaps order by the user's purpose: fix mode sorts by severity (critical first), improve mode sorts by dependency (foundations first), and refactor mode sorts by impact/effort ratio (quick wins first). Without this template, the roadmapper agent has no structure for producing purpose-appropriate brownfield roadmaps.

Output: New `get-shit-done/templates/brownfield-roadmap.md` template file with purpose-specific ordering instructions, phase structure, and coverage validation format.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@get-shit-done/templates/roadmap.md
@agents/gsd-roadmapper.md
@get-shit-done/templates/brownfield-summary.md
@.planning/phases/03-purpose-routing/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brownfield-roadmap.md template with purpose-aware ordering and phase structure</name>
  <files>get-shit-done/templates/brownfield-roadmap.md</files>
  <action>
Create `get-shit-done/templates/brownfield-roadmap.md` as a new file. This template follows the structural pattern of `get-shit-done/templates/roadmap.md` but is specialized for brownfield purpose-aware roadmaps.

The file must contain these sections in order:

**1. Header and purpose statement:**
```markdown
# Brownfield Roadmap Template

Template for purpose-aware roadmap generation from brownfield analysis. Used by `brownfield-flow.md` Step 9 (generate_roadmap) to guide the `gsd-roadmapper` agent in creating roadmaps ordered by the user's purpose.

**Key difference from greenfield roadmap:**

| Aspect | Greenfield | Brownfield |
|--------|-----------|------------|
| Phase source | Requirements from user questioning | Concerns/improvements from analysis + user purpose |
| Ordering logic | Dependency-based (foundation first) | Purpose-dependent (see below) |
| Requirement IDs | Category-based (AUTH-01, CONT-02) | Purpose-based (CONCERN-01, IMPROVE-01, REFACTOR-01) |
| Success criteria | "User can X" (new capability) | "X no longer occurs" (fix) or "User can now X" (improve) |
| Baseline | None (building from scratch) | Existing codebase (brownfield-analysis.md) |
```

**2. `<ordering_rules>` section — Purpose-specific ordering logic:**
```markdown
## Ordering Rules

### Fix Mode — Severity Order
Phases ordered by concern severity: critical first, then moderate, then minor.

**Phase derivation:**
- Group selected concerns by severity level
- Phase 1: All critical concerns (if any)
- Phase 2: All moderate concerns (if any)
- Phase 3: All minor concerns (if any)
- Within each phase, order concerns by scope of impact (more files affected = earlier)
- If only one severity level has concerns, split into 2-3 phases by functional area

**Requirement ID format:** CONCERN-01, CONCERN-02, ... (sequential, severity tagged)
**Success criteria pattern:** "[Concern description] no longer occurs" / "[Affected file] handles [scenario] correctly"

### Improve Mode — Dependency Order
Phases ordered by architectural dependency: foundations first, features that depend on them later.

**Phase derivation:**
- Identify foundation changes needed to support improvements
- Phase 1: Foundation/infrastructure changes (shared modules, APIs, data models)
- Phase 2+: Feature improvements ordered by dependency graph
- Independent improvements can be parallelized (noted in phase description)
- If improvement touches core modules, it goes in an earlier phase

**Requirement ID format:** IMPROVE-01, IMPROVE-02, ... (sequential, dependency ordered)
**Success criteria pattern:** "User can now [new capability]" / "[System] supports [new behavior]"

### Refactor Mode — Impact/Effort Ratio Order
Phases ordered by impact/effort ratio: high-impact low-effort first (quick wins).

**Phase derivation:**
- Phase 1: Quick wins — high impact, low effort (naming, conventions, small patterns)
- Phase 2: Structural changes — moderate effort, high impact (module reorganization)
- Phase 3: Deep restructuring — high effort (architecture changes)
- Impact = number of files affected + cross-document references + downstream dependencies
- Effort = single file (low) / same module (moderate) / cross-module (high) / architecture (very high)

**Requirement ID format:** REFACTOR-01, REFACTOR-02, ... (sequential, ratio ordered)
**Success criteria pattern:** "[Area] follows [target pattern]" / "[Code] uses [convention] consistently"
```

**3. File template section — The actual ROADMAP.md structure to fill:**
```markdown
## File Template

The roadmapper agent fills this template to produce `.planning/ROADMAP.md`:

---

# Roadmap: [Project Name] — [Purpose] Mode

**Version:** v1.0
**Purpose:** [Fix Issues | Add/Improve Features | Refactor/Restructure]
**Analysis Health:** [Good / Moderate / Concerning — from brownfield-analysis.md]
**Ordering:** [Severity (critical first) | Dependency (foundations first) | Impact/Effort (quick wins first)]
**Phases:** [N]
**Items:** [N] mapped

## Milestone 1: v1.0 — [Purpose-specific milestone name]

### Phase 1: [Name]
**Goal:** [Outcome, not task — what is TRUE when this phase completes]
**Items:** [CONCERN-01, CONCERN-02 | IMPROVE-01 | REFACTOR-01, REFACTOR-02]
**Ordering rationale:** [Why this phase is first — highest severity / foundational dependency / best impact-effort ratio]
**Files affected:** [file paths from analysis]

**Success Criteria:**
1. [Observable behavior confirming the phase goal is met]
2. [Observable behavior]

Plans:
- [ ] TBD (created by /gsd:plan-phase)

---

### Phase 2: [Name]
...

## Coverage

| Item | Description | Phase | Severity/Priority | Status |
|------|-------------|-------|--------------------|--------|
| CONCERN-01 | [From analysis or questioning] | 1 | critical | Pending |
| CONCERN-02 | [From analysis or questioning] | 1 | critical | Pending |
| CONCERN-03 | [From analysis or questioning] | 2 | moderate | Pending |

**Coverage:** [N]/[N] items mapped. 0 unmapped.

## Build Dependencies

[ASCII dependency graph showing phase order]
```

**4. `<guidance>` section — Instructions for the roadmapper agent:**
```markdown
## Guidance for Roadmapper Agent

When creating a brownfield roadmap:

1. **Read the purpose context** passed by brownfield-flow.md Step 9. This contains:
   - Selected purpose (fix/improve/refactor)
   - Questioning output (selected concerns, priorities, constraints, target state)
   - Analysis reference (brownfield-analysis.md path)

2. **Apply the ordering rule** matching the purpose (see Ordering Rules above).

3. **Derive phases from items, not from a template.** Let the concerns/improvements/refactoring areas determine the number and structure of phases. Don't impose "every project needs N phases."

4. **Use analysis file paths in success criteria.** Reference specific files from brownfield-analysis.md in success criteria and files-affected sections. This gives downstream planners concrete targets.

5. **Cross-check coverage.** Every item from the questioning output must map to exactly one phase. No orphans.

6. **Write ROADMAP.md, STATE.md, and update REQUIREMENTS.md** following the existing roadmapper protocol.

7. **Include ordering rationale for every phase.** The user should understand WHY this phase is sequenced where it is — not just what it contains.
```

**5. `<anti_patterns>` section:**
```markdown
## Anti-Patterns

- **Generic phase names** — "Phase 1: Fix critical issues" is too vague. "Phase 1: Fix auth bypass + N+1 query in course listing" references specific analysis findings.
- **Ignoring the ordering rule** — If purpose is "fix" but phases are ordered by dependency instead of severity, the roadmap contradicts the user's intent.
- **Success criteria without file paths** — "Auth is secure" is unverifiable. "Admin route in app/api/admin/route.ts validates session tokens" is verifiable.
- **Mixing purposes** — A fix roadmap should not include refactoring phases. If the user wants both, they should run purpose routing twice or use "Other" mode.
- **Over-splitting** — 2 concerns should not become 5 phases. Group by severity level or functional area.
```

The complete file should be approximately 120-160 lines. Ensure the template structure is compatible with the existing `gsd-roadmapper` agent's output format (ROADMAP.md with phases, success criteria, coverage).
  </action>
  <verify>
Read `get-shit-done/templates/brownfield-roadmap.md` and confirm:
1. File has header, ordering_rules, file template, guidance, anti_patterns sections
2. Ordering rules define 3 distinct modes: severity (fix), dependency (improve), impact/effort (refactor)
3. Each ordering rule specifies phase derivation logic, requirement ID format, and success criteria pattern
4. File template includes: purpose, analysis health, ordering, phases with ordering rationale, coverage table
5. Guidance section instructs roadmapper to apply purpose-specific ordering
6. Anti-patterns section warns against generic phase names and ignoring ordering rules
7. Template is compatible with gsd-roadmapper agent's existing ROADMAP.md output structure
8. Coverage table format includes severity/priority column
9. File length is approximately 120-160 lines
  </verify>
  <done>
The brownfield-roadmap.md template exists with purpose-aware ordering rules for fix (severity), improve (dependency), and refactor (impact/effort) modes. The template defines the ROADMAP.md file structure including ordering rationale per phase and a coverage table. The roadmapper agent can use this template to produce brownfield-specific roadmaps ordered by the user's selected purpose.
  </done>
</task>

</tasks>

<verification>
1. Read `get-shit-done/templates/brownfield-roadmap.md` end-to-end
2. Verify it follows the structural pattern of `get-shit-done/templates/roadmap.md` (template + guidance)
3. Verify fix mode ordering is severity-based (critical -> moderate -> minor)
4. Verify improve mode ordering is dependency-based (foundations -> features)
5. Verify refactor mode ordering is impact/effort ratio (quick wins -> deep restructuring)
6. Verify the file template section produces a valid ROADMAP.md structure
7. Verify requirement ID formats are distinct per purpose (CONCERN-, IMPROVE-, REFACTOR-)
8. Verify coverage table includes severity/priority column
9. Verify file length is approximately 120-160 lines
</verification>

<success_criteria>
- get-shit-done/templates/brownfield-roadmap.md exists with 3 purpose-specific ordering rules
- Fix mode: severity order (critical first)
- Improve mode: dependency order (foundations first)
- Refactor mode: impact/effort ratio (quick wins first)
- Template produces valid ROADMAP.md structure compatible with gsd-roadmapper
- Requirement foundation: ROUTE-04 (purpose-aware roadmap generation structure)
</success_criteria>

<output>
After completion, create `.planning/phases/03-purpose-routing/03-02-SUMMARY.md`
</output>
